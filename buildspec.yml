version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11  # Adjust based on your needs
  build:
    commands:
      - echo Building the project...
      - mvn clean package  # Adjust if you're using a different build tool
artifacts:
  files:
    - target/*.jar  # Adjust based on your project structure
